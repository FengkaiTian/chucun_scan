# 测试前审计

- **审计对象**：`strategy_final.py`、`research.py`、`RESEARCH.md`（研究员交付；协调者已于 2026-09-24 07:05 UTC 提交为 `eb969f1`，内容与审计时的工作区版本逐字节一致）
  - `strategy_final.py` sha256 `5b76a5dd4e980dc895c55571885e5e7ec579b99008b8385fa066b639f03550aa`
  - `research.py` sha256 `0e851d91a7f4619f1f0e87c3fb96fab3504fcb57b1acf76c9d3095bf4da93275`
  - `RESEARCH.md` sha256 `a5b0a0dc75e08c15f4561a6df816675ea35c149f17645363364fd13fd17eeb1f`
- **框架基准**：`09e3508`
- **审计日期**：2026-09-24。审计人未修改任何被审文件或框架文件，未创建 `FROZEN`，也未提交或推送。
- **信息隔离声明**：本审计的所有判断和概率估计只用 2025 年及以前的数据，以及仓库内的材料。审计人刻意没有使用任何关于 2026 年行情的记忆。

## 结论：**PASS**：可以冻结 `strategy_final.py`，并运行一次 2026 测试

没有必须在测试前修复的项目。审计发现研究记录有一处**实质性低估**（分红复权造成的成交额前视，见 §3.2），但它：

1. 在 2026 测试中量级极小（首个调仓日的主动权重约 0.3%），方向不利于策略；
2. 不改变候选名单，也不改变最终选择（去掉前视后，C20 仍是唯一在验证期 IR 为正的候选）；
3. 只抬高了开发期的数字，不影响一次性测试的第一类错误率。

建议把本文件与 `FROZEN` 一同提交，作为 `RESEARCH.md` 的勘误。

| # | 项目 | 结论 | 要点 |
|---|---|---|---|
| 1 | 框架未被篡改 | **通过** | 相对 `09e3508` 的 diff 为空；`FROZEN`、`results/` 不存在 |
| 2 | 策略文件纯净 | **通过** | 只 import pandas；纯函数、确定性，不修改输入；只用 `close/dvol/members` |
| 3 | 未来函数 | **存疑（不阻断）** | 代码层面无未来函数。数据层面：复权价含"调仓日之后的分红"，导致成交额前视。RESEARCH.md 称"约 1–3%"，这对**训练期**是严重低估；对 **2026 测试**则影响可忽略且方向不利于策略 |
| 4 | 可复现 | **通过** | `research.py` 全量重跑，130 行表格数字全部逐字一致；`strategy_final` 与 C20 逐日对账差 0 |
| 5 | 协议遵守与过拟合 | **通过（有小瑕疵）** | 取证确认候选在看验证期之前固定；有 4 个开发期混合试验未登记；开发期显著性经多重检验调整后不成立（DSR ≈ 0.63） |
| 6 | 测试可执行性 | **通过** | 按 `eval_2026.py` 的数据形态干跑，并加压力测试，全部无报错 |
| 7 | 2026 年 p<0.05 的独立估计 | **约 8%（5%–12%）** | 与研究员自估一致；接近检验本身约 6.6% 的实际第一类错误率 |

---

## 1. 框架完整性：通过

- `git diff 09e3508 -- harness.py eval_2026.py data_pipeline.py PREREGISTRATION.md .github data` 为空（HEAD 为 `eb969f1` 时同样为空）。
- `eb969f1` 只新增三个交付文件（688 行），不涉及框架。
- `FROZEN` 不存在，`results/` 不存在；`.github/workflows/test2026.yml` 只在推送 `FROZEN` 时触发。

## 2. 策略文件纯净性：通过

`strategy_final.py` 共 34 行。逐项检查如下：

- **导入**：只有 `import pandas as pd`。
- **IO 与网络**：无文件 IO，无网络。
- **硬编码**：没有硬编码的日期或个股。唯一的字符串常量是排除基准 `'SPY'`。
- **全局状态**：模块级常量只有不可变整数和浮点数 `CAP, WIN, HIST`；没有全局可变状态，也没有跨调用缓存。
- **输入**：只使用引擎传入的 `close, dvol, members`，不读索引日期。
- **确定性实测**（`dryrun.py` §D）：
  - 同一输入调用两次，结果完全相同；调用后输入 DataFrame 的哈希不变。
  - 打乱列顺序后权重差为 7e-18；float32 与 float64 输入的权重差为 0。
  - 价格与成交额整体乘以常数后，权重差为 5e-11（尺度不变）。
- **价位阈值**：不使用绝对价位阈值。`dvol > 0` 只是把零成交视为缺失。但权重与复权后的成交额水平**成比例**，而复权因子依赖未来分红，这一点在 §3.2 讨论。

## 3. 未来函数

### 3.1 代码层面：通过

| 行 | 计算 | 所用数据 |
|---|---|---|
| 24 | `px = close[members ∩ columns − SPY]` | 引擎截断到调仓日（含）的最近 ≤600 行 |
| 25–28 | 要求最近 253 行收盘价无缺失 | 同上，只用过去 |
| 29 | 最近 252 行成交额（`>0`）取均值 | 同上，只用过去 |
| 30–33 | 归一化，4% 上限截断后重复归一化 30 次 | 无新数据 |

代码层面没有任何未来数据。

### 3.2 数据层面：复权价含未来分红 → 成交额前视（主要发现）

**机制。** `dvol` 等于 yfinance `auto_adjust=True` 的复权收盘价乘以成交量，训练数据和 `eval_2026.py` 都这样计算。复权收盘价等于原始收盘价乘以 F(s)，其中 F(s) 是日期 s 之后直到**下载日**所有除息（以及 Yahoo 当作复权处理的分拆）因子的乘积。成交量只做拆股调整，不做分红调整。因此：

- 拆股会相互抵消。已验证：AAPL、TSLA、AMZN、NVDA、GOOGL 拆股前后的成交额均连续。
- 分红不会抵消。调仓日 t 的截面权重会乘上 F_i(t)，它约等于 exp(−股息率 × (下载日 − t))。这是**累积量**，而不是 RESEARCH.md §6.7 说的"约一个股息率、1%–3%"。

**实证（训练数据，下载日 2026-09-24）。** 用 2010-12-31 已知的原始收盘价（只做拆股换算）校准 F，结果如下：

| 股票 | AMZN | BRK-B | NFLX | GOOGL | NVDA | AAPL | MSFT | JNJ | KO | XOM | VZ | MO | T |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 复权价/原始价 | 1.000 | 1.000 | 1.000 | 0.992 | 0.915 | 0.837 | 0.758 | 0.637 | 0.619 | 0.552 | 0.446 | 0.381 | 0.250 |

也就是说，开发期早年的 C20 相对不分红股，把 T、MO、VZ 等高股息股的权重压低到 25–45%。这是用 2011–2026 年的**实际分红（含分拆）**做的前视。

**敏感性分析（审计人的近似模型）。** 每只股票的长期股息率 y 按以下方式确定：约 35 只大盘股用上表校准；已知不分红的大盘股记 0；其余按当前 GICS 行业的典型股息率；不在当前成分股表中的记 1.8%。"去前视"指把成交额乘以 exp(y·(下载日 − t))，也就是只保留调仓日之前的复权。模型是粗略的，只用于判断量级和方向。

| 版本 | 开发 IR | 开发 NW t | 开发超额 | 验证 IR | 验证 t | 全期 IR | 全期 t |
|---|---|---|---|---|---|---|---|
| C20 原样 | 0.687 | 2.38 | 2.2% | 0.335 | 0.78 | 0.539 | 2.26 |
| 去前视（完整模型） | 0.589 | 2.06 | 1.7% | 0.346 | 0.80 | 0.480 | 2.01 |
| 去前视（仅行业股息率） | 0.635 | 2.21 | 2.0% | 0.337 | 0.78 | 0.508 | 2.13 |

- **前视倾斜本身的贡献**：开发期年化 +0.23%（仅行业模型）至 +0.49%（完整模型），NW t ≈ 2.7，统计上显著；验证期约 +0.02%（t 0.1–0.3）。
- **前视造成的主动权重**（半 L1）：2009 年末 7.4%，2014 年末 5.3%，2020 年末 2.3%，2024 年末 0.7%，2025 年末 0.3%。以 2009 年末为例，高股息股（y≥3%）权重原样为 15.7%，去前视后为 22.0%；不分红股分别为 5.4% 和 3.7%。
- **对选择的影响**：用去前视数据重跑开发期排名靠前的配置（`rank_check.py`），结果为 C25 0.637、C20 0.589、C14 0.560、C02 0.557、C24 0.543、C23 0.474、C08 0.310、C21 0.264、C19 0.261。按研究员同样的去重规则，候选仍是 C25、C20、C14、C23，外加 C08/C21/C19 之一；验证期 IR 为 C20 0.342、C25 −0.119、C14 −0.149、C23 −0.203、C19 −0.634。**最终选择不变。**

**在 2026 测试中的方向与量级。** `eval_2026.py` 在运行当天（D_test）下载行情，调仓日 t 的前视部分只包括 (t, D_test] 之间的分红。

- **量级**：首个调仓日（2025-12-31）的前视主动权重在 D_test=2026-09-24 时为 0.32%，2026-12-31 时为 0.43%，2027-06-30 时为 0.64%，此后每个调仓日递减。按高、低股息组合在测试期内 ±20–30% 的收益差估算，对累计超额的影响约 0.1% 或更小；而达到 p<0.05 大约需要 5% 以上的累计超额。
- **方向**：定期分红事先公告，可预测。前视中真正含未来信息的只有分红的意外变化：削减或暂停分红、特别分红、提高分红、新开始分红，以及分拆。按构造，削减分红的股票权重被压低得较少，即相对超配；提高分红、发放特别分红和分拆的股票权重被压低得较多，即相对低配。这些事件的公告收益方向与此相反，所以信息成分在期望上**不利于**策略。剩下的是一个很小的反股息率倾斜，事前期望收益约为 0。
- **结论**：这个前视不会以有意义的方式抬高测试的第一类错误率，**不需要在测试前修正**。修正需要改动已锁定的 `eval_2026.py`（改用未复权收盘价 × 成交量），在测试前改框架的风险大于收益。
- **需要记录的勘误**：RESEARCH.md §6.7 的"约 1%–3%……训练期数据存在同样的效应……影响可忽略"不正确。训练期的效应是累积的（开发期早年高股息股的权重被压低 40–75%），使开发期 IR 虚高约 0.05–0.10，NW t 虚高约 0.2–0.3。

### 3.3 其他数据层面观察（不阻断）

- **`data/membership.csv.gz` 含 2026-01 至 2026-08 的月末成分股**：训练环境中存在 2026 年的成分股变动信息。回测只用截至 2025-12-31 的调仓日，策略也拿不到这些数据。研究员只在两处接触过它：06:16 统计成分股数量和覆盖率，以及 `research.py` 第 340 行冒烟测试的股票列表（与截至 2025 年的 `close` 列取交集，不影响结果）。**未发现利用。** 建议以后在数据管线里截断到 `TRAIN_END`。
- **分拆**：Yahoo 会把部分分拆当作复权因子（T 的 0.25、GE 约 0.36 都包含分拆）。2026 年若有成分股分拆，母公司在分拆前的调仓日会被低配，量级和方向同上，可以忽略。

## 4. 可复现性：通过

- 用 `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 python3 research.py` 全量重跑，耗时 4 分 49 秒（4 核）。输出的全部 130 行表格与 RESEARCH.md **逐字一致**，包括 IC 表、25 个配置的开发期和验证期、相关系数矩阵、C20 各期统计、逐年表、持仓集中度和功效表。
- C20 的关键数字复现为：开发期 IR 0.6865 / NW t 2.3832；验证期单独回测 IR 0.331 / t 0.769；全期连续回测的验证期切片 IR 0.3349 / t 0.7783；全期 IR 0.5388 / t 2.2554；成本拖累 0.078%/年；滚动 180 日窗口命中率为开发期 22.6%、验证期 9.6%。
- `strategy_final.target_weights` 经 `harness.backtest` 与研究配置 C20 **逐日对账，最大日收益差 0.00e+00**（脚本内有 `assert gap < 1e-10`）。审计人另行独立回测，结果相同。
- 小瑕疵：RESEARCH.md 写的是 `python3 research.py`（约 5 分钟），但研究员实际运行时设置了单线程 BLAS。不设时，4 个进程的 BLAS 线程超订，运行超过 20 分钟仍未结束（审计人中止后改用单线程重跑）。数值结果不受影响。

## 5. 协议遵守与过拟合

### 5.1 验证期只用于 5 个候选之间的选择：通过（有取证证据）

证据来自三处：研究员 agent 的会话记录（工具调用与输出；思考内容已脱敏为空）、共享 scratchpad 中的中间文件，以及 `__pycache__/research.cpython-311.pyc`。

| 时间 (UTC) | 事件 |
|---|---|
| 06:15:48 | 研究员启动 |
| 06:17–06:41 | C01–C24 逐个加入，**只在 DEV 上**评估；24 个信号的 IC 诊断（`ic*.py`）只用 2009-12 至 2020-11 |
| 06:36–06:41 | 缓存开发期收益 `dev_C*.pkl` |
| 06:41:24 | 用开发期缓存试了 5 种 50/50 混合（C20 分别配 C19/C15/C06/C10/C08，IR 为 0.734/0.658/0.194/0.288/0.437），然后定义 C25 = C20+C19 |
| **06:42:54** | 写入 `CANDIDATES = ['C25','C20','C14','C23','C19']`，同时创建 `val.py`（带 `assert k in R.CANDIDATES`），**首次**运行验证期，只跑这 5 个候选 |
| 06:42:55 | `research.cpython-311.pyc` 快照（源文件时间戳相同）：`CANDIDATES` 已定，`FINAL = None`，`CONFIGS`（25 个配置的全部参数）与最终版**完全相同** |
| 06:43–06:44 | scratchpad 中只有这 5 个候选的 `val_*.pkl`。当时输出的验证期 IR（C20 0.3309、C25 −0.1122、C14 −0.1522、C23 −0.2131、C19 −0.6342）与最终复现**逐位相同** |
| 06:45:07 | 写入 `strategy_final.py` |
| 06:46:14 | 设定 `FINAL = 'C20'` |
| 06:46:47 起 | 全量运行，此时才补算其余 20 个配置的验证期（表 4b，事后披露） |

在 06:42:54 之前，涉及 2021 年以后数据的命令只有两条结构性检查：06:16 统计成分股数量和行情覆盖率，06:18 统计 2025-12-31 有完整历史的股票数。都不涉及收益。

06:42:55 之后，`research.py` 共被修改 5 次（06:46:14、06:46:41、06:50:39、06:57:47、06:58:11）。逐条核对会话记录，这些修改只涉及 `CANDIDATES` 行之后的部分：

- `FINAL`、`CFG/NAME` 字典；
- `run` 增加可调用对象和 `cost` 参数，默认值 0.001 与引擎一致；
- IC 诊断、输出格式、覆盖率打印、冒烟测试。

位于该行之前的 `features/zs/proj/optimize/make/blend/CONFIGS` **没有被改动**。结合上面"验证期数字逐位相同"，可以确认看过验证期之后，配置和策略代码都没有改变。

> 审计过程说明：审计人在比对时 `import research`，导致 Python 用新源码重写了 `__pycache__/research.cpython-311.pyc`（gitignore 的缓存文件，不属于被审对象或框架）。原 pyc 的关键常量（`CANDIDATES`、`FINAL = None`、`CONFIGS` 全等）是在重写之前读取的，结论有效。此后基于 pyc 的函数字节码比对被重写污染，已作废，改用上述会话记录和结果比对来证明。

**旁证**：非候选 C02 的验证期 IR（0.451）高于 C20（0.331），但 C02 按开发期规则（与 C20 相关 0.94，开发 IR 更低）被剔除。如果研究员偷看过验证期，理性的选择会是 C02。

### 5.2 未登记的试验：小瑕疵，不阻断

- 06:41:24 的 5 种混合中，有 4 种（C20 配 C15/C06/C10/C08）没有作为配置登记在 RESEARCH.md 中。它们只用开发期，而且不是最终选择，影响很小，但"25 个配置"应理解为"至少 29 个组合试验"。
- 第 5 个候选（C19）是在 C21、C08、C19 之间按判断选出的。这不影响最终结果：C20 在任何合理的候选名单里都会入选，并在验证期胜出。

### 5.3 多重检验：开发期显著性不成立

以 C20 开发期为例：日超额 T=2769，年化 IR 0.687，偏度 0.04，峰度 6.7，单侧 p 0.0086。

| 口径 | 结果 |
|---|---|
| Bonferroni，N=25 / 29 / 49（含 24 个 IC 检验） | 调整后 p = 0.21 / 0.25 / 0.42 |
| 零假设下 N 个独立 t 统计量的期望最大值 | N=10 为 1.54，N=25 为 1.96，N=49 为 2.24 |
| Deflated Sharpe Ratio（Bailey & López de Prado；试验间 IR 标准差 0.29） | N=25 为 0.63，N=15 为 0.71，N=10 为 0.77，都远低于 0.95 |
| 去掉分红前视后的开发期 t | 2.06–2.21，与 N≈25 时零假设下的期望最大值（1.96）相当 |

另外还有两个问题叠加在上面：开发期存在幸存者偏差（2010 年行情覆盖率仅 68%）；验证期结果本身不显著（t 0.77）。**综合来看，C20 没有经过统计确认的优势。** 这是功效问题，不是协议违规，不影响一次性测试的有效性。研究员对此如实披露了（主动收益主要来自约 1.1 的贝塔，验证期残差 α 为负）。

### 5.4 无法证实之处

- 研究员的思考内容已脱敏，只能核查可见的命令和文本。
- 研究员（同类模型，知识截止约在 2026 年中）可能有关于 2026 年上半年行情的参数记忆，物理隔离挡不住这一点。在可见记录中没有发现任何对 2026 年实际行情的引用，只有"若 2026 年下跌……"这类条件式表述；最终选择也是由事先固定的机械规则决定的。但"配置空间的设计是否受记忆影响"无法证伪。

## 6. 测试可执行性（干跑）：通过

`dryrun.py` 完全复刻 `eval_2026.py` 的数据构造：从 2023-01-01 起，只含期末成分股，`dropna(axis=1, how='all')`，`dvol = (Close×Volume)[close.columns]`，`end = close.index[-1]`；随后依次调用 `backtest(S.target_weights, …)`、`stats`，并按 eval 的格式生成报告行。不下载数据，不写 `results/`。

| 场景 | 结果 |
|---|---|
| A. 2025 年当测试期（首个调仓日 2024-12-31） | T=250，行情覆盖 511/523，IR 0.878，NW t 0.869，p 0.192；与全样本回测的差 ≤1.2e-8（float32 与 float64 之别） |
| B. 月中结束（截至 2025-09-23，模拟 9 月下旬运行） | T=181，IR 1.206，**NW t 仅 1.01**（p 0.156）。说明 T≈180 时即使样本内 IR 达到 1.2 也不显著 |
| C. 压力测试：20 只持仓整列缺失、持仓中途退市、调仓日停牌、成交量全为 0、成交额缺失但有价、上市仅 100 日的新成分股、成分股表中有但行情里没有的代码、末行 200 列缺失 | 全程无异常；各调仓日权重合计 1.000000、最大 4%、无 NaN；异常股票都被正确排除 |

结论与观察：

- 策略对 eval 的数据形态稳健：成分股缺价只会被跳过，不会产生 `KeyError`；调仓日必有收盘价，满足引擎断言。
- **设计属性**：253 日窗口内只要缺一天价格，这只股票就会被排除最长一年。它的影响是轻微增加跟踪误差，不构成错误。
- 2026 年首个调仓日的窗口有 600 行，远多于策略需要的 253 行。

## 7. 结论与建议

**PASS。** 测试前没有必须修复的项目。

**2026 年 p<0.05 的独立估计：约 8%（区间 5%–12%）。**

- 依据：真实 IR 的主观分布取约 N(0.15, 0.35²)。理由是去掉前视并做多重检验折扣后，开发期证据很弱；验证期 IR 为 0.33 但 t 只有 0.77；超额主要来自贝塔。
- 按运行到 9 月下旬计算，T≈182–187，需要的实现 IR 约 1.9；由此得到正态近似下的通过概率约 7.3%。再计入预注册披露的 NW 小样本过度拒绝（零假设下约 6.6%），约为 8%。
- 这与研究员的自估（8%）一致，基本等于随机通过的概率。

**建议（均不阻断测试）：**

1. 把本文件与 `FROZEN` 一同提交，作为 RESEARCH.md 的勘误：§6.7 分红前视的量级，§2 少登记的 4 个混合试验，以及开发期 t 2.38 不应作为有效证据引用。
2. **尽快运行测试**，并在美股收盘数据定稿后推送 `FROZEN`，例如避开 13:30–21:00 UTC 的交易时段。原因：`eval_2026.py` 以最后一行作为 `end`，盘中运行可能纳入不完整的当日数据；同时运行越晚，首个调仓日的分红前视越大（仍然很小）。
3. 今后的框架改进（**不要在本次测试前改动**）：数据管线和 eval 用未复权收盘价 × 成交量计算成交额；`membership.csv.gz` 截断到 `TRAIN_END`；复现命令中写明 `OMP_NUM_THREADS=1`。

---

<sub>审计脚本（`div_sens.py` 分红敏感性、`rank_check.py` 去前视后的候选排序、`dryrun.py` eval 干跑、DSR 计算）放在会话 scratchpad，不属于仓库。所有结论都可以用仓库内的数据和上文描述的方法重现。</sub>

---

# 测试后复核

- **复核对象**：冻结提交 `124d002`（`FROZEN`）与结果提交 `b6f15d7`（github-actions[bot]，`results/test_2026.md`、`results/test_2026_daily.csv`、`results/wiki_test_*.html`）
- **复核日期**：2026-09-24，复核人与测试前审计是同一人。
- **约束**：复核人没有修改 `REVIEW.md` 以外的任何文件，没有提交或推送，没有在测试数据上尝试任何新策略或新参数。

## 最终裁决：**失败**（按预注册判据，未能显著跑赢标普 500）

- **判据**：预注册的成功条件是 p < 0.05 且平均超额 > 0。实现值为单侧 p = **0.0756**，所以**不成功**。
- **有效性**：未发现任何使结果无效的问题。一次性、代码冻结、数据隔离三项都得到了独立证据的支持（见下文）。
- **解读**：结果有效，应如实记为"失败"。它的意思是 2026 年的数据**不足以**拒绝"策略不跑赢 SPY"的原假设，并不证明策略没有超额。
- **后续约束**：任何后续操作都不能改变这一裁决，包括在 2026 数据上换参数、换策略、换滞后阶或换分布、延长样本后重跑、在各时间点"看一眼"之后择优等。这些都构成数据窥探，统称为多次查看（optional stopping）。无论结果如何，都**不可以作为成功证据**。

## 1. 一次性与代码冻结：通过

| 核查 | 证据 | 结论 |
|---|---|---|
| 结果只产生一次 | 抓取全部远端分支后，`git log --remotes` 中 "results:" 提交只有 `b6f15d7`；其他分支（`main`、`claude/job-search-vdowz1`）没有 `results/` 或 `FROZEN` | ✓ |
| 工作流只运行一次 | GitHub Actions `test2026.yml` 的 `total_count = 1`：run 35970715791，run_attempt 1，event push，head_sha `124d002`，07:38:33–07:39:28 UTC。此后仓库没有任何新的工作流运行 | ✓ |
| 运行的是冻结代码 | 该运行的 head_sha = `124d002`；报告里的策略 sha256 = `FROZEN` 记录 = 当前 `strategy_final.py` = `git show 124d002:strategy_final.py` = `5b76a5dd…f03550aa`，也等于测试前审计时记录的哈希 | ✓ |
| 冻结后代码未改 | `git log 124d002..HEAD -- strategy_final.py harness.py eval_2026.py data_pipeline.py` 为空；`git diff 124d002 HEAD` 对这些文件为 0 字节；框架相对 `09e3508` 的 diff 为 0 字节；研究文件相对 `eb969f1` 的 diff 为 0 字节 | ✓ |
| `124d002` 的内容 | 只新增 `FROZEN`（4 行）和 `REVIEW.md`（即测试前审计，与复核前的工作区版本一致） | ✓ |
| 运行时点 | eval 步骤在 07:38:50–07:39:24 UTC 运行，早于美股开盘（13:30 UTC）。最后一个交易日 2026-09-23 的收盘已经定稿，不存在盘中残缺行 | ✓ |
| 数据覆盖 | 日志显示 3 只下载失败：CTRA、HOLX、DAY（"possibly delisted"），因此覆盖率为 513/516。详见下文 | ✓（影响可忽略） |

**关于测试期成分股与缺失股票：**

- 用 `results/wiki_test_*.html` 离线重建测试期成分股，得到 2025-12-31 至 2026-08-31 的 9 个月末、共 516 只股票，与报告一致。
- 这 9 个月末的成分股与训练文件 `data/membership.csv.gz` 中对应月份的记录**完全相同**。
- 3 只缺失股票只在测试初期是成分股：DAY 到 2026-01，HOLX 到 2026-03，CTRA 到 2026-04。它们在训练数据中同样没有行情。
- 按典型成分股的 C20 权重（约 0.06%）估算，合计权重约 0.2–0.3%，对累计超额的影响远小于 0.1 个百分点。它们对 SPY 的影响同时计入基准，方向未知，不影响结论。

**局限**：沙箱不能联网，复核人无法独立重新下载 2026 年行情来复算每日收益本身。每日收益以冻结代码在 GitHub Actions 上唯一一次运行的输出为准；以下复算核验的是由这份输出得到的统计量。

## 2. 独立复算：与报告完全一致

输入只有 `results/test_2026_daily.csv`：182 行（2026-01-02 至 2026-09-23），无缺失，日期单调，无重复。

| 指标 | 报告 | `harness.stats` 复算 | 独立实现 |
|---|---|---|---|
| 年化收益 | 0.3020 | 0.3020 | 0.3020 |
| 基准年化 | 0.1913 | 0.1913 | 0.1913 |
| 年化波动 | 0.1647 | 0.1647 | 0.1647 |
| 最大回撤 | −0.1016 | −0.1016 | −0.1016 |
| 跟踪误差 | 0.0545 | 0.0545 | 0.0545 |
| IR | 1.7184 | 1.7184 | 1.7184 |
| NW t（滞后 ⌊4(182/100)^(2/9)⌋ = 4） | 1.4355 | 1.435535 | 1.435535 |
| 单侧 p | 0.0756 | 0.075567 | 0.075567 |

- **独立实现的做法**：不调用 harness；自己计算自协方差和 Bartlett 核长期方差，用 `statistics.NormalDist` 求 p。它与 `harness.nw_test` 的差别是 t 为 0、p 为 4e-17。
- **超额规模**：日均超额 3.72bp，年化算术超额 9.37%，累计超额（几何差）7.53 个百分点。
- **距离门槛**：p = 0.05 需要日均超额 4.26bp（年化 10.7%，约等于实现 IR 1.94）。实际实现值是门槛的 **87%**。
- **判定稳健性**：以下几种改动都不是替代检验，只用来说明结论不依赖实现细节。
  - 改用 t 分布（df = 181）：p = 0.0764。
  - 滞后阶取 0、2、4、8、12：p 分别为 0.072、0.075、0.076、0.063、0.061。
  - **所有口径的 p 都大于 0.05**，裁决不依赖检验的具体实现。按预注册，裁决只以滞后 4、正态近似的结果为准。

## 3. 探索性分析（**非预注册，不参与裁决，不得作为证据**）：β 能解释多少超额

在 2026 年日收益上估计市场模型 r = α + β·SPY + ε，标准误用 Newey-West（HAC），滞后 4：

| 量 | 值 |
|---|---|
| β | **1.184**（NW 标准误 0.028；β − 1 的 t = 6.6），高于研究期的 1.10–1.14 |
| 年化 α | 5.99%，**NW t = 1.02**（单侧 p 0.15） |
| R² / 残差年化波动 / 残差 IR | 0.912 / 4.86% / 1.23 |
| SPY 年化算术均值 | 18.4% |

各口径下 β 对超额的解释程度（年化算术超额 9.37%）：

| 口径 | (β − 1) × SPY 均值 | 占超额比例 | 剩余超额 | 剩余超额的 NW t |
|---|---|---|---|---|
| 2026 实现 β = 1.184 | 3.38% | **36%** | 5.99% | 1.02 |
| 研究期 β = 1.10 | 1.84% | 20% | 7.53% | 1.24 |
| 研究期 β = 1.14 | 2.57% | 27% | 6.79% | 1.14 |

**解读：**

- 2026 年的超额大约有 **1/5 到 1/3 可以直接由 β > 1 解释**，前提是 SPY 在这段时间上涨了约 18%（年化）。上涨日平均超额为 +17.6bp，下跌日为 −11.2bp，这种不对称正是高 β 的特征。
- 剩余部分在统计上与 0 无法区分（t ≈ 1.0–1.2）。它在方向上符合研究期对这种风格暴露的描述：超配高成交额、高关注度的大盘股，同时在 4% 上限下低配超大市值股。但**不应**据此认定存在选股 α。
- 超额在时间上集中：4 月 +2.0%、5 月 +3.5%、8 月 +2.9%、9 月（至 23 日）+1.7%；7 月 −2.8%；1–3 月小幅落后。

## 4. 裁决与说明

1. **预注册结论：失败。** p = 0.0756 ≥ 0.05（平均超额 > 0 这一条件满足）。
2. **没有发现使结果无效的问题**：
   - 一次性运行、冻结代码、测试期数据隔离、按时点重建成分股，都已独立核实；
   - 下载失败的 3 只股票影响可以忽略；
   - 测试前审计已披露的"成交额分红前视"在测试中量级约为 0.3% 的主动权重，方向不利于策略，不改变结论；
   - 预注册披露过，该检验的实际第一类错误率约 6.6%，比名义值略宽松。这只会让"成功"更容易，与本次"失败"的结论不矛盾。
3. **与事前预期一致**：测试前的独立估计是通过概率约 8%。实现 IR 1.72 已处在事前分布的右尾，但仍然没有达到约 1.94 的门槛。这说明研究设计本身统计功效低，不说明检验出了问题。
4. **数据窥探声明**：从现在起，2026 年的数据已经被"看过"。在测试集上继续调参、换策略（例如改用不设上限的 C02、改变上限或回看窗口）、换检验口径，或者延长样本到更晚的日期后重跑 `eval_2026.py`，都构成数据窥探或多次查看。这样得到的任何结果，**不论显著与否，都不可以作为成功证据**，也不能用来修改本次裁决。如果要继续研究，必须重新预注册，并使用 2026-09-23 之后、此前从未见过的新样本外区间。本节的探索性分析同样只用于描述，不构成证据。

<sub>复核脚本 `post_test.py`（复算与探索性分解）放在会话 scratchpad；GitHub Actions 的运行记录与日志通过 GitHub API 只读查询。</sub>

---

# v2 测试前审计

- **审计对象**：`PREREGISTRATION_v2.md`（`56ef29d`，21:47:55 UTC），以及修改 agent 的交付物。交付物由协调者在审计开始时提交为 `042b335`（21:56:56），工作区与该提交一致：
  - `select_v2.py` `430eea12…d28f8`
  - `eval_v2.py` `1cc2b537…ebe372`
  - `.github/workflows/test_v2.yml` `bef0670d…d481eb`
  - `results/selection_v2.json` `e1906f20…1fbde5`
  - `results/selection_v2.md` `76be6a78…19e43`
- **审计日期**：2026-09-24，审计人与 v1 审计人相同。
- **约束**：审计人没有修改任何被审文件或框架文件，没有创建 `FROZEN_V2`，也没有提交或推送；没有打开 `results/horse_race/*`。唯一涉及 2025-01-01 之后的新计算是第 6 项（仅 C20、仅 2026 年）。

## 结论：**不通过（FAIL）**：v2 不能作为有效的样本外检验冻结运行

机制层面基本合格：选择可以复现，一次性保护、sha 校验、Holm 实现和干跑都正确。问题在于推断层面，**v2 的测试期并不是"未见过的数据"**，这一点无法通过修改代码或补充披露来弥补：

1. **被选策略所属的族在测试期的表现，在写预注册之前已经基本已知。**
   - C24 与 C20 同属"成交额加权 + 4% 上限"族，开发期月度主动收益相关系数为 0.97（RESEARCH.md 表 3）。
   - C20 在 2025 年的表现已公开：RESEARCH.md 逐年表显示超额 +3.8%、年内 NW t 0.89；REVIEW.md 干跑 A 显示 IR 0.878、t 0.869。
   - C20 在 2026 年的表现就是 v1 的结果：t 1.44。
   - 协调者在预注册提交前 5 分钟（21:42:39）还算过 C20 在 2021–2026 的表现（t 1.33）和 2010–2026 的表现（t 2.61）。
   - 仅用这些已公开的数字粗略合成（不是回测），C20 在 2025-01 至 2026-09 的 NW t 约为 1.55–1.65，**恰好落在 p=0.05 的门槛上**。
2. **物理隔离已经不存在。** 赛马会话（`88d8648f`，16:08–16:46）把 2007–2026 年全部成分股的日线（`data/hr_*.parquet`）和 16 个策略的 2026 年结果推送到了本分支。从此任何人都可以在冻结前逐日计算 C24 的测试期表现，也可以自由选择推送 `FROZEN_V2` 的时间（相当于选择测试的终止日）。
3. **流程层面是"失败后继续尝试"。** 会话中挂着一个 Stop hook，条件为"用 2010–2025 找规律、用 2026 测试，只要显著超过标普 500 即成功"。v1 失败后，又先后进行了赛马（16 个策略 × 2026 年）和 v2（25 个配置 × 2025–2026 年）。用户的原话是"把所有可能性都跑一遍"。在这种情况下，v2 的 p 值不具有名义上的第一类错误率。
4. **预注册的污染披露不充分。**
   - 第 2 条只说研究团队知道"5 个候选在 2021–2025 的汇总 IR"。实际上 C20 在 2025 年的逐年数字已经写在仓库里；协调者 21:42 的计算没有披露；赛马数据与结果也没有披露。
   - 在这些条件下，p < 0.05 **不构成**"显著跑赢标普 500"的证据；p ≥ 0.05 也只是再次说明功效不足。

**建议**：不要以预注册检验的名义运行 v2。唯一干净的检验是**前瞻测试**：现在就把机械选中的 C24 连同全部代码冻结，事先固定一个未来的到期日（例如 2027-09-30 或更晚），只用 2026-10-01 以后产生的数据，到期检验一次。如果用户仍然要求运行 v2，只能作为"已知部分结果的描述性回测"报告，报告正文必须写明上述 1–4 点，并完成 §7 列出的技术修复。

| # | 项目 | 结论 | 要点 |
|---|---|---|---|
| 1 | 选择可复现、无泄漏 | **通过** | 独立复算（截断到 ≤2024-12-31）与 `selection_v2.json` 逐位一致，C24 按规则当选；`research.py` 自 `eb969f1` 以来 0 改动。第 1、2 名 t 只差 0.0155，按近似模型去掉分红前视后第一名会变成 C20 |
| 2 | 预注册先于选择 | **形式上通过，实质存疑** | 56ef29d（21:47:55）早于选择计算（21:50:47）；但预注册写在协调者算出 C20 的 2021–2026 表现之后，而且当选者所属族可以从 v1 已公开的表格预见 |
| 3 | eval_v2 与预注册一致 | **通过（有小问题）** | 一次性保护、sha 校验、主判据、Holm、spawn 都正确；终止日取"运行日（纽约日期）之前的最后一个交易日"，与"运行当天最后一个交易日"最多差 1 天；只校验 research/harness 两个文件的 sha；报告中硬编码的污染说明不完整 |
| 4 | 新增的数据清洗规则 | **存疑：可以接受，但属于偏离预注册** | 规则对策略和 SPY 对称，写于任何 v2 测试计算之前；但参照了赛马在 2026 数据上修订过的规则；**最后一个交易日的整片缺失不会被识别**（已实证） |
| 5 | 干跑 | **通过** | `eval_v2.py --dry`：25 个配置在 2023–2024 上全部跑通，耗时 19 秒，502 日，无缺失 |
| 6 | v1 数据完整性 | **v1 数据在 2026-09-22 有缺陷，裁决不变** | v1 下载时同样缺 2026-09-22：C20 持仓中 439 只（占权重 47.7%）当天无价，9-22 与 9-23 两天收益被记为 0。修正后 p 从 0.0756 变为 **0.0800**，缺陷对策略有利，失败裁决不变 |
| 7 | 结论 | **FAIL（作为确认性检验）** | 基于已公开信息，C24 达到 p<0.05 的概率约 40%（25–55%）。**这不代表策略有优势**，只说明结果大部分已知、恰在门槛附近 |

## 1. 选择可复现且无泄漏：通过

- **审计复算的方法**（`rep_select.py`，不调用 `select_v2.py`）：自行把 `train_close/train_dvol/membership` 截断到 ≤2024-12-31 并断言截断成功；配置原样复用 `research.CFG/strategy`（预注册要求使用原配置）；NW t 用自己的实现（Bartlett 核，T=3774，滞后 8）。
- **复算结果**：选中 **C24**。25 名排序与 `selection_v2.json` 中的 `ranking` **完全一致**，NW t 和 IR 的最大差为 0，p 的最大差为 6e-17。训练区间 2010-01-04 至 2024-12-31，`data_last_day_used` 为 2024-12-31。
- **前 4 名都属于成交额加权族**：C24 t 2.099、C20 2.083、C25 2.042、C02 1.978；第 5 名 C14 为 1.237。
- **当选者的稳健性（披露项）**：
  - 第 1、2 名只差 **0.0155**。
  - 按 v1 审计 §3.2 的分红前视近似模型校正（只用 ≤2024 数据）后，排序变为 C20 1.825 > C02 1.767 > C24 1.753 > C25 1.727。也就是说，机械规则选出 C24 而不是 C20，取决于一个已知的数据瑕疵。规则是机械执行的，这不构成违规，但说明"C24 优于 C20"没有实质含义。
- **配置是否被改动**：`research.py` 相对 `eb969f1` 的 diff 为 0 字节，sha256 `0e851d91…93275` 与选择记录一致；`harness.py` 相对 `09e3508` 未变（`af034bf7…2cbaa`）；框架文件相对 `09e3508` 为 0 字节。

## 2. 预注册先于选择：形式上通过，实质存疑

以下时间线来自 git 记录和会话记录，时间为 UTC：

| 时间 | 事件 |
|---|---|
| 07:39 | v1 结果：C20 在 2026 年 p=0.0756，失败 |
| 16:01–16:04 | 协调者会话（`f6190937`）多次确认失败，并表示"不会在 2026 年数据上再做调整" |
| 16:08–16:46 | 用户执行 `/clear` 后开启新会话 `88d8648f`：在 GitHub 上收集 16 个策略，推送 2007–2026 年全市场行情（`98eb69b`）和 2026 年赛马结果（`fc31754`） |
| 21:40 | 用户恢复协调者会话，问"所以本质是失败了？"，随后说"不用只拿 2026 啊，我们可以多拿几年去测试呢" |
| **21:42:39** | 协调者计算 C20：2010–2025 t 2.26；**2021–2026 t 1.33**；2010–2026 t 2.61（2021–2025 用训练数据，2026 用 v1 的日收益） |
| 21:43 | 用户选择"2010–2024 做训练，2025 和 2026 做测试……把所有可能性都跑一遍" |
| **21:47:55** | 提交 `PREREGISTRATION_v2.md`（`56ef29d`） |
| 21:48:26 | 修改 agent（即 v1 研究员，被重新唤起）接到任务 |
| 21:50:41 / 21:50:47 | 写入并运行 `select_v2.py`；结果文件时间 21:52:42 |
| 21:53:38 / 21:54:44 | `eval_v2.py` 与 `test_v2.yml` 定稿；21:56:56 提交为 `042b335` |

- **形式上**：规则在机械选择计算之前已提交，修改 agent 在预注册提交之后才接到任务。在修改 agent 和协调者的可见记录中，都没有发现预注册之前计算过 2010–2024 排名。
- **实质上**：
  - 候选集和规则虽然都是早已存在的对象，但当选者几乎可以预见：v1 已公开的开发期和验证期表格中，前几名都是成交额加权族。
  - 预注册写在协调者计算 C20 的 2021–2026 表现之后。
  - 预注册写在赛马数据和结果进入本分支之后。协调者在预注册之前的可见记录中没有读过赛马结果，但用户知道。
- **修改 agent 是否接触测试期信息**：它读过 `results/test_2026.md`（v1 结果）和赛马代码（`fetch.py`、`engine.py`、`run.py` 的开头和 grep 结果），但**没有读取** `data/hr_*.parquet` 或 `results/horse_race/*`，也没有计算任何配置在 2025 年以后的表现。它的 mock 测试只用 ≤2024 的数据（会话记录 21:48–21:55）。

## 3. `eval_v2.py` 与预注册的一致性：通过（有小问题）

| 项目 | 结论 | 说明 |
|---|---|---|
| 一次性保护 | ✓ | 正式运行时如果 `results/test_v2.md` 已存在，就在读取任何数据之前退出；工作流只在推送 `FROZEN_V2` 时触发；有 artifact 备份；推送失败时先 rebase 再推 |
| sha 校验 | ✓/△ | `research.py` 和 `harness.py` 的 sha 与 `selection_v2.json` 不符时拒绝运行。未校验 `eval_v2.py`、`select_v2.py`、`selection_v2.json` 本身和 `data_pipeline.py`；报告会记录 HEAD 与 `selection_v2.json` 的 sha，可以事后审计 |
| 主判据 | ✓ | 被选配置单侧 NW p < 0.05 且均值 > 0；T≈430 时滞后 5 阶，所需 IR 约 1.26 |
| Holm | ✓ | 单元测试通过；随机 25 个 p 值与参考实现的差为 0；拒绝条件为 Holm p < 0.05 且均值 > 0；已明确标为次要结果 |
| 测试期起点 | ✓ | 2025-01-01；首个调仓日 2024-12-31 使用测试时重建的时点成分股；下载从 2022-01-01 开始，满足 600 日回看 |
| 测试期终点 | △ | `end` = 运行时的纽约日期（yfinance 的 end 不含当天），即取运行日之前的最后一个交易日。在美股开盘前运行时与"运行当天最后一个交易日"一致；收盘后运行会少 1 天。区别很小且在事前确定，但**推送 `FROZEN_V2` 的时间本身决定了终止日**，而测试期行情任何人都能算 |
| spawn 多进程 | ✓ | 用 initializer 传入数据；子进程继承 `sys.argv`，所以 `--dry` 与测试期起点一致；干跑实测通过 |
| 报告中的污染说明 | ✗ | 硬编码只写了 3 条，缺少赛马数据与结果、21:42 的计算、C20 在 2025 年的公开数字、数据清洗规则属于偏离预注册 |

## 4. 预注册之外新增的数据清洗规则：可以接受，但属于偏离，需要披露

规则内容：
- 对有中间缺口的股票最多重新下载 3 轮，用 `combine_first` 只填补缺失值；
- 剔除 SPY 缺失的日子；
- 剔除"中间缺口占比比前后 11 日中位数高 10 个百分点以上"的日子，被剔除日的收益并入下一交易日。

评估如下：

- **对称性**：剔除日对所有序列（含 SPY）都整行删除，两日收益由 `pct_change` 合并，对策略和基准完全对称。重新下载只填补缺失值，不改动已有数据。
  - 仍然不对称的残余：低于阈值的零星缺口。harness 会把该股缺口当天和次日的收益都记为 0，而 SPY 不受影响。这是引擎本身的性质，v1 中同样存在。
- **能否被事后调成有利结果**：
  - 规则写于任何 v2 测试计算之前，修改 agent 没有计算过测试期结果。
  - 但规则的参数（11 日、10 个百分点）照搬自赛马。赛马的原始规则是"缺失 >10%"，在 2026 年数据上跑过一次后改成了现行规则。赛马代码自述"2026 年的全部数字没有变化"，本审计没有核实。
  - 由于 v2 的结果预计恰在门槛附近（见 §7），这类看似中性的处理细节可能决定裁决。第 6 项表明，9-22 的处理方式会让 C20 的 p 移动约 0.004。
- **是否偏离预注册**：是。预注册只写了"同一 `harness.backtest`"，没有规定数据清洗；v1 也没有做清洗。应在结果中列为"事前（测试计算之前）新增、未写入预注册的数据处理"。
- **缺陷：最后一个交易日的缺失不会被识别**。`holes()` 只识别前后都有数据的缺口。实证：在 ≤2024 的数据中注入 88% 缺失，放在 2024-06-03 时该日被剔除，放在最后一日 2024-12-31 时**不会被剔除**。如果 Yahoo 的缺陷恰好出现在测试最后一天，结果会像 v1 那样偏向策略。
- **意见**：规则本身方向正确。在已知的唯一实例中，它的效果是**不利于**策略的（修正了 v1 中对策略有利的缺陷），所以可以保留。前提是：(a) 在结果中作为偏离预注册披露；(b) 补上"最后一日覆盖率异常时剔除该日"的对称规则，或者把终止日固定为一个事先检查过数据完整性的日期；(c) 修改后重新审计差异。

## 5. 干跑：通过

- `python3 eval_v2.py --dry`：数据截断到 2024-12-31，测试期替换为 2023-01-03 至 2024-12-31（502 日，属于训练期内）。25 个配置全部跑通，回测耗时 18 秒（优化器类配置每个 4–5 秒），全程 19 秒。
- 输出的 `test_v2_daily.csv` 为 502 × 26 列（25 个配置 + SPY），无缺失；报告能正常生成，包括主判据、25 行全表和 Holm 结果。
- 修改 agent 的 mock 测试覆盖了下载和清洗流程：中间日 88% 缺失被正确补齐或剔除。本审计另外发现了上面第 4 项所述的末日缺口问题。

## 6. v1 数据完整性（附带）：v1 在 2026-09-22 的数据有缺陷，失败裁决不变

**方法**（`v1_integrity.py`）：用 `data/hr_close.parquet` 和 `hr_volume.parquet`（16:28–16:38 下载，auto_adjust 复权，与 eval_2026 同一口径），按 `eval_2026.py` 的方式构造数据：2023 年起、v1 测试股票池（516 只，由 `results/wiki_test_*.html` 重建）、成交额 = 收盘 × 成交量。然后用 `harness.backtest` 重算 C20 在 2026-01-02 至 2026-09-23 的日收益。只计算 C20、只计算 2026 年。

**发现：**
- 赛马数据在 2026-09-22 缺了 v1 股票池 512 只中的 **452 只**（SPY 有数据）。此外只有 FISV 在 2025-11-12 缺一天：按 253 日完整性规则它在 2026 年整年被排除，权重约 0.1%，可以忽略。
- 逐日对比：9-21 及以前，策略日收益最大差 1.3e-8，SPY 最大差 1.9e-7；**9-22 与 9-23 两天也逐位相同**（+0.4207%、−0.6080%）。这说明 v1 在 07:38 的下载**同样**缺了 2026-09-22 的这批股票。
- 缺陷的机制：2026-08-31 调仓后，C20 的 499 只持仓中有 **439 只（占权重 47.7%）** 在 9-22 没有收盘价。harness 的 `pct_change` 加 `fillna(0)` 把这些股票 9-22 和 9-23 两天的收益都记为 0，它们在这两天的实际涨跌被丢掉了。
- 修正方法：9-21 与 9-23 两天的收盘都齐全，所以把 9-22 从日历中剔除、收益并入 9-23，就能得到准确的两日收益（与 eval_v2 和赛马的处理相同）：

| 版本 | T | 9-22 至 9-23 两日策略收益 | SPY | IR | NW t | 单侧 p |
|---|---|---|---|---|---|---|
| v1 原结果 | 182 | −0.190% | −0.736% | 1.7184 | 1.4355 | 0.0756 |
| 赛马数据、保留缺口（复现 v1） | 182 | −0.190% | −0.736% | 1.7184 | 1.4355 | 0.0756 |
| **赛马数据、剔除 9-22（修正）** | 181 | **−0.377%** | −0.736% | **1.6772** | **1.4048** | **0.0800** |

**结论：**
- v1 结果文件在 2026-01-02 至 9-21 的数据有效：与另一次独立下载的差在 1e-8 量级。
- 最后两天受 Yahoo 缺陷影响，使策略相对多得约 0.19 个百分点。
- 修正后 p 为 0.080。**缺陷对策略有利，而 v1 的预注册裁决（失败）在修正后依然成立**，结论更稳。
- 更正：本文件"测试后复核"一节写有"不存在盘中残缺行"。这句话本身没错，但当时复核人只能检查结果 CSV，无法访问原始价格，**没有发现 9-22 的中间缺口**，特此更正。

## 7. 结论与建议

**裁决：FAIL（作为确认性检验）。** 机制层面的第 1、3、5 项通过，但 v2 的测试期对被选策略族来说是已知或可获取的数据，预注册的污染披露也不充分。所以按原设计冻结运行得到的 p 值不能作为"显著跑赢标普 500"的证据。这个问题无法通过修改代码来修复。

**C24 在 v2 测试期达到 p < 0.05 的概率：约 40%（25%–55%）。**
- 估计依据只有已公开的数字：C20 在 2025 年 t≈0.87–0.89、2026 年 t≈1.40–1.44，两段合成约 1.55–1.65。审计人没有计算 C24 在 2025 年以后的任何表现。
- 不确定性来自：C24 的跟踪误差缩放在测试期的具体路径、9-22 的剔除（会降低策略收益）、9-23 之后到运行日的几天，以及数据版本差异。
- **这个概率远高于 5%，原因是结果大部分已知、恰在门槛附近，而不是策略有优势。** 对于一个干净的原假设检验，这个概率应该是 5%。

**如果用户仍然要求运行 v2，必须满足以下条件：**
1. 报告标题和结论都不使用"样本外检验成功"的说法，改为"含已知数据的描述性回测（非确认性）"。
2. 在报告和 `FROZEN_V2` 提交说明中补充披露：
   - 赛马数据与结果已进入本分支（`1787a1b`、`98eb69b`、`fc31754`）；
   - 协调者在 21:42 计算过 C20 的 2021–2026 表现；
   - C20 在 2025 年的逐年数字早已写在 RESEARCH.md 和 REVIEW.md 中；
   - 数据清洗规则属于偏离预注册；
   - 第 1、2 名 t 值差 0.0155，且对分红前视敏感；
   - v1 在 9-22 的数据缺陷及修正后 p = 0.080。
3. 技术修复（修改后需要重新审计差异）：
   - 增加最后一日覆盖率检查；
   - `FROZEN_V2` 记录 `eval_v2.py`、`select_v2.py`、`selection_v2.json`、`data_pipeline.py`、`research.py`、`harness.py` 的 sha256；
   - 在 `FROZEN_V2` 中写明终止日规则，并在审计通过后立即推送，不要择时。
4. 在结果中写明累计尝试的次数：v1（1 个策略 × 2026）、赛马（16 个策略 × 2026）、v2（25 个配置 × 2025–2026）。

**推荐的替代方案**：做**前瞻测试**。现在就冻结 C24（由机械规则在 ≤2024 数据上选出）和全部代码，事先规定测试期为 2026-10-01 到一个固定的未来日期，到期一次性检验。这是在当前状态下唯一能得到有效 p 值的办法。根据预注册的功效估计，如果真实 IR 约 0.5，需要多年才有较大把握达到显著。

**数据窥探声明**：从现在起，任何根据测试期（2025–2026）表现来调整配置、规则、清洗参数、终止日或检验口径的做法，都属于数据窥探；由此得到的结果无论显著与否，都不能作为成功证据。

<sub>审计脚本 `rep_select.py`（独立复算选择与分红前视敏感性）和 `v1_integrity.py`（v1 数据完整性）放在会话 scratchpad，不属于仓库。</sub>

---

# 择时研究测试前审计

- **审计对象**（HEAD `dfc1bbb`）：
  - 框架（协调者编写）：`PREREGISTRATION_timing.md`（`71eea1a`，经 `d06e7f2` 修订、`dfc1bbb` 加勘误）、`timing/harness.py`、`timing/data.py`、`timing/eval.py`、`.github/workflows/timing_*.yml`、`data/timing_train.parquet`（`b31dd20`，由 bot 生成）；
  - 研究员交付物：`timing/strategy_final.py`、`timing/research.py`、`timing/RESEARCH.md`。
- **审计日期**：2026-09-24。审计人没有修改任何被审文件或框架文件，没有创建 `timing/FROZEN`，没有提交或推送。**本轮审计没有打开 `results/` 下任何与 2026 有关的文件，也没有打开 `data/hr_*`**；审计中用到的"2026 年走势"信息仅限于本文件"测试后复核"一节已公开的月度数字。

## 结论：**PASS**：可以冻结 T02 并运行一次 2026 年检验（附带披露要求和一项强烈建议的修复）

与 v2 不同，这一轮的策略**选择过程没有受到 2026 年信息的影响**：
- 候选、选择规则和判据都在任何 2026 年相关决定之前固定；
- T02 是在"1961–1997 为主测试"的设定下，按机械规则选出的；
- 把主测试改为 2026 年，是用户按自己最初的设计提出的，不是根据研究结果做的决定；
- 而且这次改动对策略**不利**（见 §5）。

2026 年是已经看过的行情，因此即使检验成功，证据力度也很弱。这一点已在预注册中披露，还需在结果中补充几项说明（见 §7）。

| # | 项目 | 结论 | 要点 |
|---|---|---|---|
| 1 | 框架未被研究员篡改 | **通过** | 框架文件只在 71eea1a、d06e7f2、dfc1bbb 中出现，均由协调者提交；研究员只写了自己的三个交付文件；训练数据止于 2025-12-31，没有任何 2026 年日期 |
| 2 | 策略纯净 | **通过** | 只 import numpy，只用 `df['tr']`；没有 IO、日期常量或历史事件；仓位 ∈ {0, 1} ⊂ [0, 1.5]；结果确定 |
| 3 | 未来函数 | **通过** | 用不经过引擎的向量化实现重算，逐日收益与引擎完全相同（差 0），时序为"第 t 日收盘的信号决定第 t+1 日的收益"。有两项执行上的理想化假设需要披露 |
| 4 | 可复现 | **通过** | `python3 -m timing.research` 耗时 27 秒，66 行表格与 RESEARCH.md **全部逐字一致**；T02 按规则当选（+0.111）；`strategy_final` 与 T02 逐日对账差为 0 |
| 5 | 协议与时间线 | **通过（勘误需加强措辞）** | 候选在 22:40:21 写入，并在同一条命令中只对候选跑验证期；T02 在 22:40:2x 当选，修订提交于 22:41:00。修订由用户驱动，且让成功**更难**。勘误中的"很可能"应改为"确定" |
| 6 | eval 可执行性 | **通过（有风险）** | 离线 mock 干跑了三种情形。**风险**：附加参考先于主检验计算，Shiller 和 French 同时不可用时，eval 在计算 2026 之前就会崩溃（已实证） |
| 7 | 结论 | **PASS** | 2026 年 K1/K2/K3 任一成立的概率，审计人独立估计约 **3%（1%–8%）**；研究员自估约 10% |

## 1. 框架完整性：通过

- **各文件的提交记录**：
  - `timing/harness.py`、`timing/data.py`、`timing_data.yml`：只在 `71eea1a`；
  - `timing/eval.py`、`timing_test.yml`：`71eea1a` 和 `d06e7f2`；
  - `PREREGISTRATION_timing.md`：`71eea1a`、`d06e7f2`、`dfc1bbb`（勘误）。
- **作者**：会话记录显示，以上改动都由协调者完成（22:34–22:35 编写框架，22:40:49 修订，22:46:04 加勘误）。研究员的工具调用（22:36:52–22:45:47）只写过 `timing/research.py`、`timing/strategy_final.py` 和 `timing/RESEARCH.md`，没有改动任何框架文件。`5aaf76e` 是协调者对研究员进行中文件做的留档快照。
- **训练数据**：`data/timing_train.parquet` 由 bot 在 `b31dd20` 生成，7042 行，1998-01-05 至 2025-12-31，没有任何 2026 年日期，没有缺失值；`tr` 与 `cumprod(1+ret)` 完全一致。
- 工作区与 HEAD 一致；`timing/FROZEN` 不存在。

## 2. 策略纯净：通过

`strategy_final.py` 只导入 numpy。只读 `df['tr']`；没有 IO、日期常量、个股或历史事件；唯一常量是 `N = 200`。输出为 `float(tr[-1] > mean(tr[-200:]))`，历史不足 200 行时输出 1.0，取值 ∈ {0, 1}。函数没有状态，结果确定。

## 3. 未来函数：通过

- **引擎**（`harness.backtest`）：循环 `i` 从 s−1 到 e−2，策略只拿到 `df.iloc[max(0, i−599) : i+1]`（截至第 i 天收盘），该仓位赚第 i+1 天的 `ret` 和 `rf`，成本按 `|w − w_prev|` 计。
- **独立验证**：用 pandas 向量化实现 T02（第 t 日信号 = `tr_t > 最近 200 日均值`，`shift(1)` 后作用于第 t+1 日），在 2000–2020 和 2021–2025 两段上与引擎的逐日收益**差为 0**，仓位序列完全相同。
- **研究配置中的其他信号**：`sma_month` 只用已完成月份（`iloc[:-1]`），`tsmom` 和 `vol_scale` 也只用窗口内的过去数据，都没有未来函数。
- **需要披露的理想化假设（不是未来函数）**：
  - (a) 用第 t 日收盘价计算信号，并且在同一个收盘价成交。现实中需要用收盘集合竞价单，并接受少量偏差；
  - (b) 现金赚的是第 t+1 日的 ^IRX 收盘利率（当日利率），影响可以忽略。
- **复权**：SPY 使用 auto_adjust 的收益率（精确的总收益），信号是总收益指数与其均线之比，与尺度无关。不存在选股 v1 那种"成交额 × 复权尺度"的前视问题。

## 4. 可复现：通过

- `python3 -m timing.research`（4 核，27 秒）输出 66 行表格，与 `timing/RESEARCH.md` **逐字一致**，包括训练期 15 个配置、验证期候选和非候选、T02 各期统计以及逐年表。
- 程序内的 `assert pick == FINAL` 和 `assert gap < 1e-12` 都通过：T02 按"验证期夏普差最大"当选（+0.111），`strategy_final` 与 T02 在训练和验证期逐日一致。
- T02 的关键数字：训练期 K3 成立（最大回撤 −23.4% 对 −55.2%，回撤差 p 0.000；收益更差 p 0.205）；验证期三条都不成立（夏普差 +0.111，p 0.33）；2025-12-31 收盘信号为满仓。

## 5. 协议与时间线：通过（勘误需加强措辞）

下表时间为 UTC，来源是协调者和研究员的会话记录以及 git 记录：

| 时间 | 事件 |
|---|---|
| 22:27:16 | **用户**提出设计：训练 2000–2020，验证 2021–2025，**以 2026 年为独立测试** |
| 22:29:57 | 协调者提醒 2026 年已看过（"设计规则时……会偏向一直满仓的规则，因为我知道 2026 年没有大跌"） |
| 22:34–22:35:41 | 协调者**自行**把主测试改为 1961–1997，提交框架和预注册 `71eea1a`（判据 K1–K3 此时已固定） |
| 22:36:47 | 研究员开始工作 |
| 22:38:37–22:40:04 | 训练期回测 T01–T15（分四批） |
| **22:40:21** | 写入 `CANDIDATES = [T02, T03, T08, T12, T15]`，同一条命令中运行验证期（`val_run.py` 内有 `assert c in T.CANDIDATES`），T02 以 +0.111 胜出 |
| 22:40:22 | **用户**："所以我说的你没听吗？按我说的做很难吗" |
| 22:40:49 / **22:41:00** | 协调者按用户要求把主测试改回 2026 年（`d06e7f2`） |
| 22:41:22 | 研究员执行 `git pull`，才得知修订 |
| 22:41–22:45 | 研究员写 `strategy_final.py`、设 `FINAL = 'T02'`、写 RESEARCH.md；此后配置、候选和信号代码都没有变化（与 `5aaf76e` 快照的 AST 比对：`sma/sma_month/tsmom/vol_scale/make/CONFIGS/CANDIDATES` 完全相同） |
| 22:45:49 | 研究员回报；22:46:04 协调者加勘误 |

- **候选先于验证期固定**：已证实。
- **修订是否利用了 2026 年的信息**：
  - 修订发生在 T02 当选**之后**，这一点是确定的，不只是"很可能"。
  - 但修订由用户驱动：用户在 22:27 就要求用 2026 年，22:40 再次坚持。协调者在 22:36:50 到 22:40:49 之间没有任何工具调用，研究结果直到 22:45:49 才回报，所以修订**不可能基于研究结果**。
  - 研究员本轮没有读取任何 2026 年的数据文件，只用了训练数据。
- **修订是否让选择变得"对 2026 有利"**：没有。
  - 选择规则（验证期夏普差）和候选都与测试集无关，修订前后没有变化。
  - T02 是趋势过滤，在已知的 2026 年上涨年里，它要么与基准完全相同（三条都不可能成立），要么在回调低点附近离场、在更高处回场，从而吃亏。
  - 研究员自己估计的成功概率：以 1961–1997 为主测试时，K3 约 50%；以 2026 年为主测试时约 10%。这次修订让成功变得**更难**。
- **勘误需要加强措辞**：勘误说"很可能也在 T02 当选之后"，应改为"**确定**在 T02 当选之后（验证期结果于 22:40:2x 输出）"；训练期回测实际持续到 22:40:04（T15），而不只是 22:38–22:39。这不影响结论。
- **多重比较**：15 个配置、5 个候选；验证期夏普差在候选之间大多是噪声（p 0.33–0.73），T02 当选有相当的偶然性。研究员已如实披露。

## 6. eval 可执行性：通过（有风险）

方法（`timing_dry.py`）：把被审代码复制到临时目录后运行，不写入仓库。mock 了 `timing.data.close`（yfinance）以及 Shiller 和 French 的网络请求：
- "2026"用训练数据平移日期得到，对应真实的 2024 年或 2025 年；
- 1957–1997 用训练期收益平铺成合成价格；
- ^IRX 在 1960 年以前无数据（与 Yahoo 一致）。

| 情形 | 结果 |
|---|---|
| A. Shiller 路径；"2026" = 真实的 2025 年 1–9 月 | 全程跑通；报告格式正确（主检验表、K1–K3 结论、附加参考段）；三个输出文件都生成。Shiller 月份映射正确（1950.1 → 10 月）。顺带观察：在这段有 −18.8% 急跌的行情中，T02 满足 K3（回撤 −9.6% 对 −18.8%，p 0.0125），说明 K3 在约 180 日内**是可能成立的**，前提是出现一次深而缓的回撤 |
| B. French 退回路径；"2026" = 真实的 2024 年（T02 全年满仓） | 跑通；附加参考标为"French 美股市场总收益（预注册备选）"。退化情形下策略与基准完全相同：超额的 NW t 和 p 显示为 nan（numpy 除以 0 得 nan，不会崩溃），三条判据都判为不成立，结论正确 |
| C. Shiller 和 French 都不可用 | **eval 在计算附加参考时抛出 ConnectionError，2026 年主检验根本没有运行**，`results/` 为空 |
| 再次运行 | 结果文件已存在时，拒绝运行 ✓ |

**风险与建议**：
1. **附加参考阻断主检验（强烈建议在冻结前修复，不属于阻断项）**：`eval.py` 先计算 1961–1997 的参考，再计算 2026。只要 Shiller 和 French 都不可用，或者 1957–1997 的 ^GSPC/^IRX 下载失败，主检验就会随之失败。
   - 由于崩溃发生在任何 2026 年计算之前，不会泄露结果，修复后可以重跑，因此不影响有效性；
   - 但冻结后再修改框架是不好的做法。建议协调者在冻结**前**把主检验移到前面，并用 try/except 包住附加参考（失败时在报告中注明）。这是与结果无关的改动，改后请把差异交给审计复核。
2. **Shiller 的地址和格式无法离线实测**：`http://www.econ.yale.edu/~shiller/data/ie_data.xls` 可能已经失效（Shiller 的数据已迁往新站点），这时会按预注册退回 French，属于合规路径。更危险的情况是文件还在但格式变了：解析可能不报错却读错列，导致附加参考悄悄出错。建议加一个合理性检查（例如 1961–1997 年的股息率应在 1%–7% 之间，否则退回 French）。French 的 zip 结构在 mock 中验证通过，但真实地址同样无法实测。以上都只影响附加参考，不影响主结论。
3. **终止日**：`end = pd.Timestamp.today()`（运行机的 UTC 日期，yfinance 不含当天），在任何时间运行都不会纳入盘中未完成的 K 线。择时只下载 SPY 和 ^IRX，受 Yahoo 批量缺失（2026-09-22 那类问题）影响的可能性很小；即使某天缺失，`dropna` 也会把收益并入下一日，对策略和基准是对称的。

## 7. 结论

**PASS：可以冻结 `timing/strategy_final.py`（T02），并运行一次 2026 年检验。**

**2026 年 K1/K2/K3 任一成立的概率，审计人独立估计约 3%（1%–8%）。**
- 依据是已公开的 2026 年 SPY 月度收益（本文件"测试后复核"一节）：前三个季度是强势上涨，只有 3 月一次中等幅度的 V 形回调（当月 −4.9%，4 月 +10.5%）。审计人**没有**在 2026 年数据上计算 T02。
- 这种路径下，T02 要么全程在场，与基准相同，三条都不成立；要么在回调低点附近跌破均线离场、在更高处回场，回撤并没有显著更浅，收益反而更差，K3 也难以成立。
- K3 真正可能成立的情形，是像 2025 年那样的深而缓的下跌（见 §6 情形 A），而已知的 2026 年路径不属于这种情形。
- 研究员自估约 10%，偏乐观但处在同一量级。
- **这个估计本身用到了测试期信息，这正说明 2026 年作为测试集的证据力度很弱。**

**结果中需要额外披露的事项（除预注册已有的内容外）：**
1. 修订"主测试改为 2026"**确定**发生在 T02 当选之后；修订由用户驱动，而且对策略不利；请按 §5 修正勘误措辞。
2. 2026 年是本仓库第 4 次在 2026 年行情上做检验：选股 v1、赛马 16 个策略、v2 的设计，以及本次择时。如果本次成功，应按累计尝试次数折扣看待。
3. K1 和 K3 衡量的是风险调整收益和回撤，**不是**"收益显著超过标普 500"。即使仅凭 K3 成功，也不代表原目标（显著跑赢标普 500）达成。
4. 执行上的理想化假设：信号价与成交价都是同一个收盘价，现金赚当日利率。
5. 附加参考 1961–1997 的数据来源（Shiller 或 French）以及它可能失败的情况。

<sub>审计脚本 `timing_dry.py`（离线 mock 干跑，三种情形）放在会话 scratchpad，不属于仓库；复现输出为 `timing_out.md`。</sub>

### 复核：冻结前修改（`4581eb1`）——OK

- **修改内容**：`git diff dfc1bbb 4581eb1 -- timing/ PREREGISTRATION_timing.md` 只涉及三处，`timing/harness.py`、`timing/strategy_final.py`（sha256 `d681956b…40605`，未变）和工作流都没有改动：
  - (a) `eval.py` 先算 2026 年主检验，并立即写出 `timing_2026_daily.csv`；1961–1997 附加参考整体包在 try/except 中，失败时报告写明原因、不影响主结论；报告说明中补齐了 §7 要求的披露（第 4 次在 2026 年行情上检验、K1/K3 不等于收益显著超过标普 500、收盘成交和当日利率两项理想化假设）；
  - (b) `data.py` 的 Shiller 分支加了股息率中位数 1%–7% 的检查，不通过时抛出异常，被既有的 except 接住，按预注册退回 French；
  - (c) 勘误改为"**确定**发生在 T02 当选之后"，并注明修订由用户推动。三处改动都与结果无关，符合审计建议。
- **干跑重跑**（`timing_dry.py`，mock 网络，测试期 = 真实的 2025 年或 2024 年，不接触任何 2026 数据）：
  - **Shiller 正常**：通过，附加参考标为"^GSPC + Shiller 股息"；
  - **Shiller 格式异常**（新增情形，股息率中位数 0.236）：检查拦下，退回 French，通过；
  - **French 退回**（全年满仓的退化情形）：通过，统计量显示 nan，裁决正确；
  - **两者都不可用**：**2026 年主检验照常完成**，`timing_test.md` 与 `timing_2026_daily.csv` 都写出，附加参考一节写明"计算失败（不影响主检验）：ConnectionError"，报告格式正确。
  - 在前三种使用同一测试期的情形下，主检验的日度 CSV 与报告主体**逐字节相同**，说明主结论与附加参考走哪条路径无关。再次运行时一次性保护仍然生效。
- **剩余的小问题（不阻断）**：`timing_2026_daily.csv` 先于 `timing_test.md` 写出，而一次性保护只检查后者。如果进程在两者之间被杀（例如附加参考的下载卡住直到任务超时），理论上可以重跑。但这种情况下没有提交任何内容，报告也还没打印，不会泄露结果，因此可以接受。如有需要，可以给 `timing_test.yml` 加上 `timeout-minutes`。
- **结论**：择时研究测试前审计维持 **PASS**，可以创建 `timing/FROZEN` 并运行一次。

---

# 择时研究测试后复核

- **复核对象**：冻结提交 `16c0fc2`（`timing/FROZEN`）、训练数据恢复提交 `8aa9a69`，以及结果提交 `3f11d24`（github-actions[bot]）中的 `results/timing_test.md`、`results/timing_2026_daily.csv`、`results/timing_test_daily.csv`。
- **复核日期**：2026-09-24，复核人与测试前审计相同。
- **约束**：复核人没有修改任何被审文件，没有提交或推送。安慰剂分析使用了已公开的 2026 年结果，只用来评估检验本身，**不**据此修改策略或判据。

## 裁决摘要

| 问题 | 结论 |
|---|---|
| (i) 按预注册机械规则 | **成功**：K3 成立（回撤差自助法 p = 0.0020 < 0.0167，收益更差 p = 0.177 > 0.05）；K1、K2 不成立。计算正确，一次性执行，代码已冻结 |
| (ii) K3 这次"成立"是否可信 | **不可信，不构成择时能力的证据。** K3 的自助法对最大回撤差**严重失准**：在同一条 2026 路径上，毫无择时能力的随机 12 天空仓规则有 **9.4%** 通过 K3（名义 1.67%）；按正确的随机化检验，T02 的回撤改善 **p = 0.123**。p = 0.0020 是"以观测值为中心"的自助法用在极值统计量上的产物 |
| (iii) 原目标"收益显著超过标普 500" | **未达成**：T02 年化 12.6%，标普 19.1%；年化超额 −5.8%（K2 p = 0.82）；夏普 0.74 对 1.11 |
| (iv) 1961–1997 附加参考 | K3 同样失准（随机规则 26.5% 通过），K1 的 p = 0.013 紧贴门槛。但随机化检验显示，T02 的夏普改善和回撤改善**都超过全部 200 条随机规则**（p < 0.005）。这与 Brock, Lakonishok & LeBaron (1992) 等文献中已知的均线择时降风险性质一致，属于**文献层面已知的事实**，不是新证据，也不计入成败 |

## 1. 一次性与冻结：通过

- **只运行一次**：`timing_test.yml` 的运行记录 `total_count = 1`（run 36070296828，run_attempt 1，event push，22:58:02–22:58:40 UTC）。结果提交只有 `3f11d24`，它只新增了三个 `results/` 文件。
- **运行时的代码版本**：该次运行的 `head_sha` 是 `8aa9a69` 而不是 `16c0fc2`，因为两个提交是一起推送的，而路径过滤命中了其中的 `timing/FROZEN`。报告里记录的代码提交也是 `8aa9a69`。
  - `16c0fc2` 到 `8aa9a69` 之间只改动了 `data/timing_train.parquet`；
  - 这个文件的内容与研究时的 `b31dd20` **完全相同**（`git diff` 为空），而 `eval.py`、`harness.py`、`data.py`、`strategy_final.py` 都不读取它，eval 会自己下载数据。
  - `6bbd400` 是 `4581eb1` 修改 `data.py` 时连带触发的数据重建（截止 2025-12-31，只有舍入差异），已被 `8aa9a69` 还原，对测试没有影响。
- **冻结内容**：`strategy_final.py` 的 sha256 为 `d681956b…40605`，报告记录值、`timing/FROZEN` 记录值和当前文件三者一致。`16c0fc2` 之后，`timing/` 下的代码、`PREREGISTRATION_timing.md` 和 `.github/` 都没有改动。
- **测试区间**：2026-01-02 至 2026-09-23，共 182 日。测试在 22:58 UTC 运行，当时 9-24 的收盘已经确定，但按事先审计过的代码规则（`end` = 运行日 UTC 日期，yfinance 不含当天），9-24 不在测试区间内。这与"运行日最后一个完整交易日"的字面表述差 1 天，属于事前固定的规则，偏保守，不影响有效性。

## 2. 独立复算：与报告一致

输入只有两个日度 CSV。CSV 中没有 rf：审计人用空仓日的策略收益反推 rf（换仓日加回 5bp），满仓日按前后插值。这只影响夏普的绝对值（K1），不影响 K2 和 K3。

**CSV 内部一致性**：满仓且未换仓的日子，策略收益与基准完全相同；进场日等于基准减 5bp，差为 0。

**T02 在 2026 年的仓位**：只有 **1 段、12 个交易日**空仓，从 2026-03-23 到 04-08，共切换 2 次。

| 指标（2026） | 报告 | `harness.criteria` 原样调用 | 独立实现（自写 NW / MDD / 几何块长平稳自助法，随机数不同） |
|---|---|---|---|
| 年化收益 / 基准 | 0.1260 / 0.1913 | 0.1260 / 0.1913 | 0.1260 / 0.1913 |
| 夏普 / 基准夏普 | 0.7431 / 1.1086 | 0.7469 / 1.1121（rf 近似） | 夏普差 −0.365 |
| 夏普差 p | 0.8715 | 0.8715 | 0.866 |
| 超额 NW t / p；收益更差 p | −0.9256 / 0.8227；0.1773 | 相同 | 相同 |
| 最大回撤 / 基准 | −0.0652 / −0.0888 | 相同 | 相同，回撤差 0.0236 |
| 回撤差 p | 0.0020 | 0.0020 | 0.0030 |
| 判据 | K1 ✗ K2 ✗ K3 ✓ | 相同 | 相同 |

**1961–1997**：K2 和 K3 的数字逐位一致（回撤 −16.55% 对 −45.07%，回撤差 p 0.0000，收益更差 p 0.7616）。夏普差 p 复算为 0.0140（报告 0.0130），两者都略低于 0.0167，但**与门槛的距离只有 1–1.5 个自助法蒙特卡洛标准误**（B = 2000 时约 0.0025），K1 成立非常脆弱。

## 3. K3 检验的校准（关键）

预注册披露的"自助法实际误判率约 3.5%"只针对**均值差**做过验证，**没有验证最大回撤差**。审计人在测试前审计中也没有检查这一点，这是审计的疏漏。下面补做校准（脚本 `timing_placebo.py`）。

**(a) 2026 年同一路径上的安慰剂（穷举）**
- 做法：T02 的空仓结构是"1 段 × 12 日"，在 182 日中放一段连续 12 日空仓，共有 171 种位置，全部穷举。每一种都原样调用 `harness.criteria`（B = 2000，seed = 0）。
- 结果：
  - **K3 成立的比例为 9.4%**（16/171）。回撤差 p < 0.0167 的比例为 9.9%。K1、K2 成立的比例都是 0。
  - 这些"成功"的随机规则，空仓段起点都集中在 2026-02-25 到 03-27，也就是恰好覆盖了 3 月那次回调。
  - **随机化 p（回撤差 ≥ T02 的 0.0236）= 21/171 = 0.123**；同样放置的规则中，夏普差不低于 T02 的比例为 0.865。
  - 换句话说，只要在 2 月底到 3 月底之间任意空仓 12 天，就有很大机会通过 K3。T02 的离场时点在无技能规则中排在前 12% 左右，远谈不上显著。

**(b) 2000–2020 年训练期的安慰剂（实际第一类错误率）**
- 做法：取 243 个 182 日窗口（每 21 日一个起点），每个窗口随机放 10 个空仓段，对每条规则跑 K1/K2/K3（`boot_p` 原样调用，B = 500）。

| 空仓结构 | K3 误判率 | 按窗口内基准最大回撤分组：≤ −15% / −15%~−7% / > −7% |
|---|---|---|
| 1 段 × 12 日（与 2026 年的 T02 相同） | **3.1%**（K1 0.2%，K2 0%） | 3.5% / 3.4% / 2.3% |
| 1 段 × 45 日（≈ T02 长期平均空仓比例 26%） | **4.5%** | **8.5%** / 4.0% / 1.2% |

名义水平为 1.67%。实际误判率高出 2–5 倍，而且在有回撤的窗口中更高。2026 年那种"短而急、呈 V 形"的回调对 K3 尤其"友好"，所以 2026 年路径上的误判率更高，达到 9.4%。

**(c) p = 0.0020 从何而来**
- `boot_p` 采用"以观测值为中心"的检验：p = P\*(d\* − d̂ ≥ d̂)。这相当于假设 d\* − d̂ 的自助分布能近似 d̂ − d 的抽样分布。
- 对最大回撤差这个量，这一假设不成立：
  - T02 在 2026 年的 d\* 分布：均值 0.0080，中位数 0.0006，P95 0.024，P99 0.041，最大 0.054；**35.5% 的自助路径中 d\* 恰好为 0**。
  - 原因是块重抽样把"空仓的 12 天"和"那一次回撤"的对应关系打散了，于是大多数自助路径里空仓日根本没有落在回撤段内。
  - 自助分布因此系统性地远低于观测值（偏差约 −0.016）。中心化之后，检验实际上要求 d\* ≥ 2·d̂ = 0.047，这种情况只在 0.2% 的路径中出现，于是得到 p = 0.0020。
  - 如果不做中心化，P\*(d\* ≥ d̂) = 0.143，与随机化 p 0.123 相近。
- 从理论上看，最大回撤是路径上的极值泛函，而自助法对极值类统计量本来就不一致（经典的例子是 Bickel & Freedman 1981 讨论的样本最大值）；再加上"暴露与事件的对齐"被重抽样破坏，以观测值为中心的块自助法**不适合**用来检验回撤差。
- 更合适的做法，是把随机化检验（保持暴露结构，打乱时点）当作零假设分布，或者对回撤差检验事先做安慰剂校准。
- 以上是**事后的探索性分析**，只用来判断检验本身是否可信，**不改变**预注册的机械结论。

## 4. 裁决

1. **按预注册机械规则：成功**（K3 成立）。数据、代码、一次性执行都已核实，没有发现使结果无效的操作性问题。
2. **结合校准结果：K3 这次"成立"不可信**，不应被当作 T02 具有择时能力的证据。
   - 检验本身在这类数据上的实际误判率远高于名义值：同路径 9.4%，训练期 3–5%，有回撤的窗口最高 8.5%。
   - 按正确的零假设（随机择时、暴露结构相同），T02 的回撤改善 p = 0.123。
   - 整个"成功"来自一次 12 天的离场：3-20 收盘跌破均线后离场，4-08 收盘后回场。回撤少了 2.4 个百分点，代价是这 182 日的累计收益少了约 4.5 个百分点（年化 12.6% 对 19.1%）。
   - 这次还是本仓库第 4 次在 2026 年行情上做检验，按累计尝试次数也应进一步折扣。
   - 测试前审计给出的 3% 通过概率，同样没有考虑到这种失准，特此说明。
3. **用户最初的目标"收益显著超过标普 500"：未达成。** 2026 年 T02 的收益显著**低于**买入持有的可能性更大：超额 −5.8%/年，K2 p = 0.82，收益更差 p = 0.18。K3 和 K1 衡量的是回撤和风险调整收益，与"收益超过"是两回事。
4. **1961–1997 附加参考（不计入成败）**：
   - 200 条随机规则（T02 的 83 段空仓长度随机打乱、随机放置，共空仓 2288 日）中，**K3 有 26.5% 通过**（回撤差 p < 0.0167 的有 43%），说明 K3 的自助法检验在这段数据上同样严重失准；K1 的通过比例为 0%（随机空仓会拉低夏普，K1 在这个零假设下偏保守）。
   - 但 T02 本身明显优于随机择时：夏普差 0.253，而随机规则的均值为 −0.081、最大值为 0.140；回撤差 0.285，而随机规则的均值为 0.064、最大值为 0.195。**两项的随机化 p 都小于 0.005**。
   - 结论：在 1961–1997 年，200 日均线规则确实比"同样空仓比例的随机择时"有更好的风险调整收益和更浅的回撤。收益高 1.7 个百分点/年，但超额不显著（K2 p 0.24）。
   - 这与 Brock, Lakonishok & LeBaron (1992) 及后续文献记录的均线择时降风险性质一致。由于这段历史和这条规则在文献中早已广为人知（预注册披露第 1 条），它只能算"复现了已知文献结论"，不是新的样本外证据，也不改变本研究的成败判定。

**建议**：对外表述时，应写"按预注册规则判定为成功（仅 K3），但事后校准显示 K3 检验在该数据上失准，这一结果不构成择时能力的证据；收益显著超过标普 500 的目标未达成"。以后任何以回撤类统计量为判据的研究，都应在预注册阶段先用安慰剂或随机化方法完成校准。

<sub>复核脚本：`timing_post.py`（从 CSV 复算，含独立实现）、`timing_placebo.py`（安慰剂与随机化校准），放在会话 scratchpad，不属于仓库。</sub>
